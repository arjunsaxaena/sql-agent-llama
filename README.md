# sql-agent-llama
An AI agent which takes in user query in natural language, searches the database autonomously, gives response in natural language

## Setup

1. Install dependencies:
   `pip install -r requirements.txt`
2. Copy `env.example` to `.env` and fill the values.
3. Run:
   `python main.py`
